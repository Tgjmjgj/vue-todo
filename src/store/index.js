import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

/*
interface ListEntry {
  header: string;
  content: string;
  creationTime: Date;
  completionTime: Date | null;
  deleted: bool;
};

state.data <-- ListEntry[];
*/

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
